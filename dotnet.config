# .NET 10 runs test projects through Microsoft.Testing.Platform rather than VSTest.
# xunit.v3 targets that platform, so `dotnet test` needs the opt-in here.
[dotnet.test:runner]
name = "Microsoft.Testing.Platform"
